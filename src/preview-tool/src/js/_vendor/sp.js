var classSet = require('./class-set');
var validator = require('./validator');
var Message = antd.message;

var SP = {
    classSet: classSet,
    validator: validator,
    message: Message
};

module.exports = SP;
